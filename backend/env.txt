# Default to SQLite for local dev (file stored in backend/steward.db)
DATABASE_URL=sqlite:///./steward.db

# To switch to Postgres later, comment above and uncomment below:
# DATABASE_URL=postgresql+psycopg2://steward:stewardpass@localhost:5432/stewarddb
